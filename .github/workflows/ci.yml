name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    # Weekly (Mon 06:00 UTC) — catch upstream drift (Homebrew/tool changes)
    # even without a commit, so the kit doesn't silently bit-rot.
    - cron: "0 6 * * 1"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: shellcheck + syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: bash syntax (3.2-safe parse)
        run: |
          for f in install.sh linux/install.sh uninstall.sh linux/uninstall.sh lib/*.sh scripts/*.sh scripts/qa/*.sh config/zshrc.block.sh linux/config/zshrc.block.sh; do
            bash -n "$f"
          done
      - name: shellcheck
        run: shellcheck -x -S warning -e SC2154 install.sh uninstall.sh lib/common.sh scripts/*.sh

  lint-linux:
    name: linux shellcheck + syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: bash syntax
        run: |
          for f in linux/install.sh linux/uninstall.sh linux/scripts/*.sh linux/config/zshrc.block.sh; do
            bash -n "$f"
          done
      - name: shellcheck
        run: shellcheck -x -S warning -e SC2154 linux/install.sh linux/uninstall.sh linux/scripts/*.sh

  lane3-qa:
    name: lane 3 fake-home QA
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: run lane 3 scripted QA
        env:
          AI_BOILER_PLATE_HOME: ${{ runner.temp }}/ai-boiler-plate-home
        run: |
          chmod +x scripts/qa/*.sh scripts/*.sh .githooks/pre-commit .githooks/pre-push
          scripts/qa/goal-mode-gate.sh --quick
          scripts/qa/lane3-all.sh

  linux-e2e:
    name: linux install -> uninstall (ubuntu)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: install (non-interactive; skip Docker; HERMES off for speed)
        env:
          HERMES: "0"
          # mise resolves versions via api.github.com; anonymous requests share
          # the runner IP's 60/hr limit and flake — authenticate with the job
          # token (5000/hr) instead.
          GITHUB_TOKEN: ${{ github.token }}
        run: ./linux/install.sh --yes --skip docker

      - name: verify installed
        run: |
          set -uo pipefail
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$HOME/.bun/bin:$HOME/.local/share/mise/shims:$PATH"
          fail=0
          need() { command -v "$1" >/dev/null 2>&1 && echo "ok   $1" || { echo "MISS $1"; fail=1; }; }
          for t in rg fd bat fzf jq gh starship mise uv bun rustup zsh; do need "$t"; done
          for t in node go python; do
            mise which "$t" >/dev/null 2>&1 && echo "ok   mise:$t" || { echo "MISS mise:$t"; fail=1; }
          done
          command -v ast-grep >/dev/null 2>&1 && echo "ok   ast-grep" || { echo "MISS ast-grep"; fail=1; }
          for t in codex claude; do need "$t"; done
          grep -q 'ai-boiler-plate:main' "$HOME/.zshrc" && echo "ok   zshrc block" || { echo "MISS zshrc block"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/state.json" && echo "ok   state.json" || { echo "MISS state.json"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/latest-report.md" && echo "ok   latest-report.md" || { echo "MISS latest-report.md"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/manual/index.html" && echo "ok   manual/index.html" || { echo "MISS manual/index.html"; fail=1; }
          exit $fail

      - name: uninstall (teardown)
        run: ./linux/uninstall.sh --yes --skip docker

      - name: verify removed
        run: |
          if grep -Eq 'ai-boiler-plate|bss-ai-boilerplate' "$HOME/.zshrc" 2>/dev/null; then
            echo "STILL zshrc block"; exit 1
          fi
          echo "ok   zshrc block removed"
          export PATH="$HOME/.local/bin:$PATH"
          if command -v claude >/dev/null 2>&1; then echo "STILL claude"; exit 1; fi
          echo "ok   claude removed"

  linux-e2e-distros:
    # The README claims Fedora/Arch/openSUSE are verified end-to-end; this job
    # makes CI actually cover them (Ubuntu is covered by linux-e2e above).
    name: linux install -> uninstall (${{ matrix.image }})
    runs-on: ubuntu-latest
    container: ${{ matrix.image }}
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        image:
          - fedora:latest
          - archlinux:latest
          - opensuse/tumbleweed
    steps:
      # These minimal base images ship bash/curl/tar/gzip but NOT git. Install
      # git so actions/checkout uses git (its REST-tarball fallback would also
      # work via tar+gzip, but git is more robust). Containers run as root and
      # have no sudo — the kit's SUDO="" root path handles that fine.
      - name: bootstrap container (git for checkout)
        shell: bash
        run: |
          set -eux
          if command -v dnf >/dev/null 2>&1; then
            dnf install -y git tar gzip
          elif command -v pacman >/dev/null 2>&1; then
            # Arch: a bare 'pacman -Sy' is a partial-upgrade hazard, so do a full
            # -Syu (the installer runs the same upgrade anyway under --yes).
            pacman -Syu --noconfirm git tar
          elif command -v zypper >/dev/null 2>&1; then
            # Tumbleweed is rolling: the container snapshot regularly drifts
            # behind the mirrors, and mid-publish mirror syncs then 404 on the
            # exact RPM versions the stale metadata references. `dup` aligns
            # the container with the current snapshot first (a few minutes,
            # but it removes the dominant flake in this job).
            zypper --non-interactive dup || zypper --non-interactive refresh
            zypper --non-interactive install git tar gzip
          fi
          # opensuse/tumbleweed's image config defines NO PATH env. Once a later
          # installer (bun/uv/rustup in the kit's install step) prepends to
          # GITHUB_PATH, the runner rebuilds the step PATH from that empty base
          # and `docker exec` can no longer even find `sh` (exit 127). Seed the
          # system dirs via GITHUB_PATH (the runner ignores PATH set through
          # GITHUB_ENV) so every later rebuild keeps them.
          echo "$PATH" | tr ':' '\n' >> "$GITHUB_PATH"

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: install (non-interactive; skip Docker; HERMES off for speed)
        env:
          HERMES: "0"
          # mise resolves versions via api.github.com; anonymous requests share
          # the runner IP's 60/hr limit and flake — authenticate with the job
          # token (5000/hr) instead.
          GITHUB_TOKEN: ${{ github.token }}
        run: ./linux/install.sh --yes --skip docker

      - name: verify installed
        run: |
          set -uo pipefail
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$HOME/.bun/bin:$HOME/.local/share/mise/shims:$PATH"
          fail=0
          need() { command -v "$1" >/dev/null 2>&1 && echo "ok   $1" || { echo "MISS $1"; fail=1; }; }
          for t in rg fd bat fzf jq gh starship mise uv bun rustup zsh; do need "$t"; done
          for t in node go python; do
            mise which "$t" >/dev/null 2>&1 && echo "ok   mise:$t" || { echo "MISS mise:$t"; fail=1; }
          done
          command -v ast-grep >/dev/null 2>&1 && echo "ok   ast-grep" || { echo "MISS ast-grep"; fail=1; }
          for t in codex claude; do need "$t"; done
          grep -q 'ai-boiler-plate:main' "$HOME/.zshrc" && echo "ok   zshrc block" || { echo "MISS zshrc block"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/state.json" && echo "ok   state.json" || { echo "MISS state.json"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/latest-report.md" && echo "ok   latest-report.md" || { echo "MISS latest-report.md"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/manual/index.html" && echo "ok   manual/index.html" || { echo "MISS manual/index.html"; fail=1; }
          exit $fail

      - name: uninstall (teardown)
        run: ./linux/uninstall.sh --yes --skip docker

      - name: verify removed
        run: |
          if grep -Eq 'ai-boiler-plate|bss-ai-boilerplate' "$HOME/.zshrc" 2>/dev/null; then
            echo "STILL zshrc block"; exit 1
          fi
          echo "ok   zshrc block removed"
          export PATH="$HOME/.local/bin:$PATH"
          if command -v claude >/dev/null 2>&1; then echo "STILL claude"; exit 1; fi
          echo "ok   claude removed"

  lint-windows:
    name: windows powershell parse
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: parse all .ps1 (PowerShell is preinstalled on the runner)
        shell: pwsh
        run: |
          $fail = 0
          Get-ChildItem -Path windows -Recurse -Include *.ps1 | ForEach-Object {
            $tokens = $null; $errors = $null
            $null = [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors)
            if ($errors.Count -gt 0) {
              $fail = 1
              Write-Host "FAIL $($_.FullName)"
              $errors | ForEach-Object { Write-Host "  $($_.Message) (line $($_.Extent.StartLineNumber))" }
            } else { Write-Host "ok   $($_.Name)" }
          }
          exit $fail
      - name: PSScriptAnalyzer (warnings+errors)
        shell: pwsh
        run: |
          Install-Module PSScriptAnalyzer -Force -Scope CurrentUser
          $res = Invoke-ScriptAnalyzer -Path windows -Recurse -Severity Warning,Error
          $res | Format-Table -AutoSize
          if ($res | Where-Object { $_.Severity -eq 'Error' }) { exit 1 }

  dryrun:
    name: macOS dry-run
    runs-on: macos-14
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - run: ./install.sh --dry-run
      - run: ./uninstall.sh --dry-run --yes

  integration:
    name: macOS install -> uninstall
    runs-on: macos-14
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: install (non-interactive)
        # plain run: non-tty so Colima start is auto-declined; everything else runs.
        # HERMES=0 skips the heavy external Hermes installer to keep CI lean
        # (its wiring is still covered by the dry-run job).
        env:
          GITHUB_TOKEN: ${{ github.token }}  # authenticated mise version lookups
        run: HERMES=0 ./install.sh

      - name: verify installed
        run: |
          set -euo pipefail
          eval "$(/opt/homebrew/bin/brew shellenv)"
          export PATH="$HOME/.local/share/mise/shims:$HOME/.bun/bin:$HOME/.local/bin:/opt/homebrew/opt/rustup/bin:$PATH"
          fail=0
          need() { command -v "$1" >/dev/null 2>&1 && echo "ok   $1" || { echo "MISS $1"; fail=1; }; }
          for t in mise starship rg fd fzf bat gh colima mo node python go rustc codex claude; do need "$t"; done
          echo "node:   $(node -v)"
          echo "python: $(python --version)"
          echo "go:     $(go version)"
          echo "codex:  $(codex --version 2>/dev/null | head -1 || echo '?')"
          test -d "$HOME/.oh-my-zsh"            || { echo "MISS oh-my-zsh"; fail=1; }
          grep -q 'ai-boiler-plate:main' "$HOME/.zshrc" || { echo "MISS zshrc block"; fail=1; }
          test -d "$HOME/.codex/plugins/cache/sisyphuslabs/omo" && echo "ok   lazycodex/omo" || echo "warn lazycodex/omo not found"
          test -f "$HOME/.ai-boiler-plate/state.json" && echo "ok   state.json" || { echo "MISS state.json"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/latest-report.md" && echo "ok   latest-report.md" || { echo "MISS latest-report.md"; fail=1; }
          test -f "$HOME/.ai-boiler-plate/manual/index.html" && echo "ok   manual/index.html" || { echo "MISS manual/index.html"; fail=1; }
          exit $fail

      - name: uninstall (full teardown)
        run: ./uninstall.sh --yes

      - name: verify removed
        run: |
          set -euo pipefail
          eval "$(/opt/homebrew/bin/brew shellenv)" || true
          export PATH="$HOME/.local/share/mise/shims:$HOME/.bun/bin:$HOME/.local/bin:$PATH"
          fail=0
          gone() { command -v "$1" >/dev/null 2>&1 && { echo "STILL $1"; fail=1; } || echo "gone $1"; }
          for t in codex claude starship mise; do gone "$t"; done
          test -d "$HOME/.oh-my-zsh" && { echo "STILL oh-my-zsh"; fail=1; } || echo "gone oh-my-zsh"
          if grep -Eq 'ai-boiler-plate|bss-ai-boilerplate' "$HOME/.zshrc" 2>/dev/null; then echo "STILL zshrc block"; fail=1; else echo "gone zshrc block"; fi
          test -x /opt/homebrew/bin/brew && echo "ok   Homebrew preserved" || { echo "Homebrew missing!"; fail=1; }
          exit $fail

  windows-e2e:
    name: windows install -> uninstall
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: check winget (preinstalled on windows-2025 image)
        shell: pwsh
        run: winget --version

      - name: install (non-interactive; skip Docker Desktop; Hermes n/a on Windows)
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}  # authenticated mise version lookups
        run: .\windows\install.ps1 -Yes -Skip docker

      - name: verify installed
        shell: pwsh
        timeout-minutes: 5
        run: |
          $env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' +
                      [Environment]::GetEnvironmentVariable('Path','User') + ';' +
                      "$env:USERPROFILE\.cargo\bin;$env:USERPROFILE\.bun\bin;$env:USERPROFILE\.local\bin;$env:LOCALAPPDATA\mise\shims"
          $fail = 0
          function need($c) {
            if (Get-Command $c -ErrorAction SilentlyContinue) { Write-Host "ok   $c" }
            else { Write-Host "MISS $c"; $script:fail = 1 }
          }
          foreach ($t in 'git','gh','jq','rg','fd','bat','fzf','starship','mise','uv','rustup','bun') { need $t }
          $shimDir = Join-Path $env:LOCALAPPDATA 'mise\shims'
          foreach ($t in 'node','go','python') {
            $candidates = @(
              (Join-Path $shimDir "$t.exe"),
              (Join-Path $shimDir "$t.cmd"),
              (Join-Path $shimDir $t)
            )
            if ($candidates | Where-Object { Test-Path $_ }) {
              Write-Host "ok   mise shim:$t"
            } else {
              Write-Host "warn mise shim:$t not found"
            }
          }
          $env:Path += ";$env:LOCALAPPDATA\mise\shims;$env:USERPROFILE\.bun\bin;$env:USERPROFILE\.local\bin"
          foreach ($t in 'codex','claude') {
            if (Get-Command $t -ErrorAction SilentlyContinue) { Write-Host "ok   $t" }
            else { Write-Host "MISS $t"; $fail = 1 }
          }
          $prof = $PROFILE.CurrentUserAllHosts
          if ((Test-Path $prof) -and (Select-String -Path $prof -SimpleMatch 'ai-boiler-plate:main' -Quiet)) {
            Write-Host "ok   profile block"
          } else { Write-Host "MISS profile block"; $fail = 1 }
          $helperHome = Join-Path $env:USERPROFILE '.ai-boiler-plate'
          foreach ($path in @(
            (Join-Path $helperHome 'state.json'),
            (Join-Path $helperHome 'latest-report.md'),
            (Join-Path $helperHome 'manual\index.html')
          )) {
            if (Test-Path $path) { Write-Host "ok   $path" }
            else { Write-Host "MISS $path"; $fail = 1 }
          }
          [Environment]::Exit($fail)

      - name: uninstall (teardown)
        shell: pwsh
        run: .\windows\uninstall.ps1 -Yes

      - name: verify removed
        shell: pwsh
        run: |
          $prof = $PROFILE.CurrentUserAllHosts
          if ((Test-Path $prof) -and (Select-String -Path $prof -Pattern 'ai-boiler-plate|bss-ai-boilerplate' -Quiet)) {
            Write-Host "STILL profile block"; exit 1
          }
          Write-Host "ok   profile block removed"
          if (Test-Path "$env:USERPROFILE\.local\bin\claude.exe") {
            Write-Host "STILL claude"; exit 1
          }
          Write-Host "ok   claude removed"
          [Environment]::Exit(0)
